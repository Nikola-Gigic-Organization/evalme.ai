import schemas from "./schemas";
import type { Lists } from ".keystone/types";
import type { GraphQLSchemaExtension } from "@keystone-6/core/types";

export const lists: Lists = schemas;
export const extendGraphqlSchema = [];
