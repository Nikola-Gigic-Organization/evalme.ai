query GetAllTopics {
  topics(orderBy: { title: asc }) {
    id
    slug
    title
    description
    questions {
      id
      title
      text
      viewerAnswer
    }
    tags {
      id
      name
    }
  }
}
